import { HighlightUpcoming } from './highlight-upcoming';

describe('HighlightUpcoming', () => {
  it('should create an instance', () => {
    const directive = new HighlightUpcoming();
    expect(directive).toBeTruthy();
  });
});
